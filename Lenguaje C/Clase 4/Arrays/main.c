#include <stdio.h>
#include <stdlib.h>
#include "Biblioteca.h"

int main()
{
    int cantidadNumeros;

    /*printf("Cuantos numeros desea ingresar? ");
    scanf("%d", &cantidadNumeros);

    if(cantidadNumeros > 0)
    {
    }

    int numerosUsuario[5];

    for(int i=0;i<5;i++)
    {
        numerosUsuario[i] = PedirEntero("Ingrese un numero");
    }

    int aux;

    for(int i=0;i<4;i++)
    {
        for(int j = i;j<5;j++)
        {    //      5                   3
            if(numerosUsuario[i] > numerosUsuario[j])
            {
                aux = numerosUsuario[i];
                numerosUsuario[i] = numerosUsuario[j];
                numerosUsuario[j] = aux;
            }
        }
    }

    for(int i=0;i<5;i++)
    {
        printf("\n%d", numerosUsuario[i]);
    }
*/
    char texto[2] = "Hola";


    printf("%s", texto);

    return 0;
}
