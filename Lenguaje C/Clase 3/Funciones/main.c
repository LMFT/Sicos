#include <stdio.h>
#include <stdlib.h>
#include "Biblioteca.h"
//Declaracion de funciones
// tipo de retorno, nombre de la funcion, (parametros de entrada)

//Tipos de retorno
    // Los tipos de dato que ya vimos (int, float, char, etc)
    // void



int numeroUsuario;

    //Funciones y bibliotecas
int main()
{
    int comparacion;
    int primerNumero;
    int segundoNumero;
    int numeroUsuario;
    int numeroUsuario;

    primerNumero = PedirEntero("Ingrese el primer numero");
    //printf("%d", )    return 0;
}

int CompararDosNumeros (int primerNumero, int segundoNumero)
{
    if(primerNumero < segundoNumero)
    {
        return -1;
    }
    else
    {
        if(primerNumero == segundoNumero)
        {
            return 0;
        }
        return 1;
    }
}


//Implementacion


